#pragma once

namespace SaturnMath
{
    /**
     * @brief Specifies the order for sorting operations
     */
    enum class SortOrder
    {
        Ascending,  ///< Sort in ascending order (smallest to largest)
        Descending  ///< Sort in descending order (largest to smallest)
    };
}
